/**:
  ros__parameters:
    # Loads map/<current_map>/<current_map>_semantic.yaml once when
    # RobotStatus.current_map changes. Fixed regions are legacy fallback only.
    enable_region_fallback: false
    # name,min_x,min_y,max_x,max_y
    semantic_regions:
      - "loading_area,-14.50,12.00,-13.00,13.50"
      - "charging_area,-12.80,12.00,-11.20,13.50"
      - "dispatch_area,-10.80,12.00,-9.20,13.50"
      - "waiting_area,-14.50,9.80,-13.00,11.40"
    status_update_sec: 1.0
